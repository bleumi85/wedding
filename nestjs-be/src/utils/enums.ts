export enum AgeType {
  ADULT,
  CHILD,
}

export enum Gender {
  MALE,
  FEMALE,
}

export enum MealRequest {
  NONE,
  PESCETARIAN,
  VEGETARIAN,
  VEGAN,
}

export enum NodeEnv {
  DEV = 'development',
  PROD = 'production',
}

export enum ResponseStatus {
  OPEN,
  CONFIRMED,
  CANCELED,
}

export enum Role {
  ADMIN,
  GUEST,
  WITNESS,
}
